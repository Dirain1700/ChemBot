"use strict";

import type { ClientOptions, Snowflake } from "discord.js";

export const RSC_TOKEN: string = "";

export const options: ClientOptions = {
    intents: [],
};

export const admin: Snowflake[] = [];
